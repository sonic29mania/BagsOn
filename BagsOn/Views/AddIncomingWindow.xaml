<Window x:Class="BagsOn.Views.AddIncomingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Надходження товару"
        Height="557"
        Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F3F6FC">

    <Window.Resources>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="24"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="22"
                                      ShadowDepth="0"
                                      Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0 0 0 5"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12 0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="150"/>
            <Setter Property="Background" Value="#5B8DEF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="SecondaryButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="Width" Value="120"/>
        </Style>

    </Window.Resources>

    <Grid Margin="24">

        <Border Style="{StaticResource CardStyle}">

            <StackPanel>

                <TextBlock Text="Надходження товару"
                           FontSize="25"
                           FontWeight="Bold"
                           Foreground="#111827"
                           Margin="0 0 0 8"/>

                <TextBlock Text="Вкажіть кількість товару, яка надійшла на склад."
                           FontSize="14"
                           Foreground="#6B7280"
                           TextWrapping="Wrap"
                           Margin="0 0 0 22"/>

                <TextBlock Text="Товар"
                           Style="{StaticResource LabelStyle}"/>

                <TextBlock x:Name="ProductInfoTextBlock"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="#111827"
                           TextWrapping="Wrap"
                           Margin="0 0 0 14"/>

                <TextBlock Text="Поточна кількість"
                           Style="{StaticResource LabelStyle}"/>

                <TextBlock x:Name="CurrentQuantityTextBlock"
                           FontSize="15"
                           FontWeight="SemiBold"
                           Foreground="#111827"
                           Margin="0 0 0 18"/>

                <TextBlock Text="Кількість надходження"
                           Style="{StaticResource LabelStyle}"/>

                <TextBox x:Name="QuantityTextBox"
         Margin="0 0 0 14"
         MaxLength="6"
         PreviewTextInput="OnlyDigits_PreviewTextInput"
         DataObject.Pasting="OnlyDigits_Pasting">

                    <TextBox.InputScope>
                        <InputScope>
                            <InputScope.Names>
                                <InputScopeName NameValue="Number"/>
                            </InputScope.Names>
                        </InputScope>
                    </TextBox.InputScope>

                </TextBox>

                <TextBlock Text="Коментар"
                           Style="{StaticResource LabelStyle}"/>

                <TextBox x:Name="CommentTextBox"
                         Height="80"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         Margin="0 0 0 22"/>

                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right">

                    <Button Content="Скасувати"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Margin="0 0 10 0"
                            Click="CancelButton_Click"/>

                    <Button Content="Додати"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Click="SaveButton_Click"/>

                </StackPanel>

            </StackPanel>

        </Border>

    </Grid>
</Window>